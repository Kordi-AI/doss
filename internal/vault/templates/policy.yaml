# Disclosure rules: who can be told what. Default: nothing leaves.
# give: full (as is) | rough (the fact file's `rough:` field) | yes-no (boolean only) | nothing
# First matching rule wins, top to bottom — keep the catch-all last.

groups:
  # kordi-friends: [kordi:pedro, kordi:qiancx]

rules:
  # - about: profile.dietary
  #   to: kordi-friends
  #   give: full
  # - about: calendar.*
  #   to: kordi-friends
  #   give: yes-no
  - about: "*"
    to: anyone
    give: nothing
